============
Installation
============

At the command line::

    $ easy_install django-filepond

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-filepond
    $ pip install django-filepond
